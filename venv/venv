#!/usr/bin/env bash

case $SHELL in
*/zsh) 
    BASH_PATH="${(%):-%x}"
    ;;
*/bash)
    BASH_PATH="${BASH_SOURCE[0]}"
esac

BASH_Path=$(dirname $BASH_PATH)
python3 $BASH_Path/venv.py $@

tmpFile=$PWD/.venv/tmp
if [ -f "$tmpFile" ]; then
    . $tmpFile
    rm $tmpFile
fi

